{
  "name": "@hidock/model-host",
  "version": "0.1.0",
  "description": "HiDock Model Host: runs the model tools on the machine that has the GPU and lends them over the LAN",
  "private": true,
  "type": "module",
  "main": "./src/main.mjs",
  "scripts": {
    "start": "node src/main.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:installer": "node scripts/build-installer.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  },
  "license": "MIT"
}
