node_modules/
dist/
out/
coverage/
logs/
*.log
.env
.env.*
!.env.example
.venv*/
__pycache__/
*.tsbuildinfo
.DS_Store
/docs/assets/*-raw.png
.playwright-cli/
# Local performance captures include private database/profile snapshots.
/artifacts/startup-*/

# Git Bash turns `2>NUL` into a file named NUL in the working directory.
# Claude Code's shell snapshot does this on every start (unalias pkill 2>NUL).
NUL
