# HiDock Next 2.0 UI 日本語化 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** HiDock Next 2.0 の UI を、英語を残したまま日本語に切り替えられるようにする。

**Architecture:** react-i18next を導入し、機能ごとのネームスペースに分けた翻訳カタログを持つ。言語設定は既存のテーマ機構（プリペイントの bootstrap → Zustand ストア → `config.json` へのミラー）と同じ形に揃える。英語カタログの抽出と日本語翻訳を別のタスクに分け、既存テストの 814 個の英語文字列アサートを「表示を壊していない」ことの回帰網として使う。

**Tech Stack:** TypeScript / React 19 / Electron / electron-vite / Zustand / Vitest / i18next / react-i18next

**Spec:** `docs/superpowers/specs/2026-09-22-i18n-ja-design.md`

## Global Constraints

- 作業ブランチは `feat/i18n-ja`。既定ブランチ `main` に直接コミットしない。
- **`npm run check:native` を実行しない。** この開発機では失敗するうえ `packages/database/node_modules/better-sqlite3` のバイナリを消したまま復元しない。壊した場合の復旧は `cd packages/database && npm rebuild better-sqlite3`。
- 回帰判定の基準値は `npm run typecheck` が EXIT 0、`npm run test:run` がテストファイル 397 個中 395 個成功・テスト 5,625 件中 5,622 件成功。
- 既知の赤 3 件（タイムゾーン起因、本作業と無関係）は除外して判定する。該当は `electron/main/services/__tests__/retrieval-orchestrator.test.ts` の 2 件と `src/lib/__tests__/calendar-utils.test.ts` の 1 件。
- **英語カタログは一字一句そのまま抽出する。** 言い回しの改善・表記ゆれの統一・誤字の修正を一切行わない。それらは英語の回帰網を無効化する。
- 翻訳キーの命名は `<namespace>:<component>.<slug>`。
- ネームスペースは `common` / `layout` / `library` / `device` / `settings` / `today` の 6 つ。
- コミットメッセージは 1 行目 `種別: 要約`、本文に「なぜ」を書く。日本語で書く。
- コミットメッセージの末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` を付ける。
- 全作業ディレクトリは `apps/electron`。

---

### Task 1: i18n 基盤の導入

i18next を入れ、空のカタログで初期化できるところまでを作る。まだどのコンポーネントも翻訳しない。

**Files:**
- Modify: `apps/electron/package.json`（dependencies に 2 件追加）
- Create: `apps/electron/src/i18n/index.ts`
- Create: `apps/electron/src/i18n/locales/en/common.json`
- Create: `apps/electron/src/i18n/locales/ja/common.json`
- Create: `apps/electron/src/i18n/__tests__/i18n.test.ts`
- Modify: `apps/electron/src/test/setup.ts`（末尾に追記）

**Interfaces:**
- Consumes: なし（最初のタスク）
- Produces:
  - `initI18n(lng: 'en' | 'ja'): typeof i18n` — i18next インスタンスを初期化して返す。二重呼び出しは言語の切り替えのみ行う
  - `SUPPORTED_LANGUAGES: readonly ['en', 'ja']`
  - `NAMESPACES: readonly ['common', 'layout', 'library', 'device', 'settings', 'today']`

- [ ] **Step 1: 依存を追加する**

```bash
cd apps/electron
npm install i18next react-i18next
```

`package.json` の `dependencies` に 2 件入ったことを確認する。

- [ ] **Step 2: 失敗するテストを書く**

`apps/electron/src/i18n/__tests__/i18n.test.ts`:

```ts
/**
 * i18n core — initialisation contract shared by the app entry point and the
 * Vitest setup file. Both call initI18n(); it must be safe to call twice.
 */

import { describe, it, expect } from 'vitest'
import { initI18n, SUPPORTED_LANGUAGES, NAMESPACES } from '../index'

describe('initI18n', () => {
  it('initialises with the requested language', () => {
    const i18n = initI18n('en')
    expect(i18n.language).toBe('en')
  })

  it('is safe to call twice and switches the language on the second call', () => {
    initI18n('en')
    const i18n = initI18n('ja')
    expect(i18n.language).toBe('ja')
    initI18n('en')
  })

  it('falls back to English for a key missing from the Japanese catalogue', () => {
    const i18n = initI18n('ja')
    // `fallbackLng: 'en'` means an untranslated key renders the English string
    // rather than the raw key, so a partial ja catalogue is never user-visible
    // as gibberish.
    expect(i18n.options.fallbackLng).toEqual(['en'])
    initI18n('en')
  })

  it('exposes the supported languages and namespaces', () => {
    expect(SUPPORTED_LANGUAGES).toEqual(['en', 'ja'])
    expect(NAMESPACES).toEqual(['common', 'layout', 'library', 'device', 'settings', 'today'])
  })
})
```

- [ ] **Step 3: テストが失敗することを確認する**

```bash
cd apps/electron && npx vitest run src/i18n/__tests__/i18n.test.ts
```

期待: `Failed to resolve import "../index"` で FAIL。

- [ ] **Step 4: 空のカタログを作る**

`apps/electron/src/i18n/locales/en/common.json`:

```json
{}
```

`apps/electron/src/i18n/locales/ja/common.json`:

```json
{}
```

- [ ] **Step 5: 初期化コードを書く**

`apps/electron/src/i18n/index.ts`:

```ts
/**
 * i18n core — the single place i18next is configured.
 *
 * Two callers initialise it: src/main.tsx (the app, pre-paint, with the
 * language the user persisted) and src/test/setup.ts (Vitest, always 'en').
 * Calling initI18n() more than once must therefore be harmless: the second and
 * later calls only switch the active language.
 *
 * Catalogues are imported statically rather than lazy-loaded. They are small,
 * and a synchronous init is what lets main.tsx render the correct language on
 * the first paint instead of flashing English.
 */

import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'

import enCommon from './locales/en/common.json'
import jaCommon from './locales/ja/common.json'

export const SUPPORTED_LANGUAGES = ['en', 'ja'] as const
export type SupportedLanguage = (typeof SUPPORTED_LANGUAGES)[number]

export const NAMESPACES = ['common', 'layout', 'library', 'device', 'settings', 'today'] as const

const resources = {
  en: { common: enCommon },
  ja: { common: jaCommon }
}

export function initI18n(lng: SupportedLanguage): typeof i18n {
  if (i18n.isInitialized) {
    if (i18n.language !== lng) void i18n.changeLanguage(lng)
    return i18n
  }

  void i18n.use(initReactI18next).init({
    lng,
    fallbackLng: 'en',
    ns: NAMESPACES,
    defaultNS: 'common',
    resources,
    // React escapes interpolated values itself; letting i18next escape them too
    // double-encodes apostrophes and ampersands in the English catalogue.
    interpolation: { escapeValue: false },
    // A key with no translation renders the English string (fallbackLng). In
    // development we also want to know about it.
    saveMissing: false,
    missingKeyHandler: import.meta.env.DEV
      ? (_lngs, ns, key) => console.warn(`[i18n] missing key: ${ns}:${key}`)
      : undefined,
    react: {
      // Catalogues are bundled, so nothing suspends. Turning this off keeps
      // components out of Suspense boundaries they do not otherwise need.
      useSuspense: false
    }
  })

  return i18n
}

export default i18n
```

- [ ] **Step 6: テストが通ることを確認する**

```bash
cd apps/electron && npx vitest run src/i18n/__tests__/i18n.test.ts
```

期待: 4 件 PASS。

- [ ] **Step 7: Vitest のセットアップで英語固定にする**

`apps/electron/src/test/setup.ts` の末尾（158 行目の後）に追記する:

```ts
// i18n — every suite runs in English. The existing 814 getByText()/findByText()
// assertions across 64 files assert the English strings verbatim, so they double
// as proof that the extraction into catalogues did not change a single rendered
// string. A test that wants Japanese calls initI18n('ja') itself and restores
// 'en' afterwards.
initI18n('en')
```

同ファイルの import 群（`import { vi } from 'vitest'` の下）に追加する:

```ts
import { initI18n } from '@/i18n'
```

- [ ] **Step 8: 全テストが基準どおりであることを確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck は EXIT 0。テストはファイル 397 個中 395 個成功、テスト 5,625 件中 5,622 件成功（既知の赤 3 件のみ）。i18n のテスト 4 件が増えるため、実際の数は 401 ファイル / 5,629 件になる。**既知の赤 3 件以外が赤くなっていたら、そこで止めて原因を調べる。**

- [ ] **Step 9: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/package.json apps/electron/package-lock.json \
        apps/electron/src/i18n apps/electron/src/test/setup.ts
git commit -F - <<'EOF'
feat: i18n 基盤として i18next を導入する

カタログは静的 import で束ねる。遅延ロードにすると main.tsx が
プリペイントで同期的に言語を確定できず、英語が一瞬見えてから
日本語に切り替わるため。

テスト環境は initI18n('en') で英語固定にする。既存テストが英語文字列を
814 箇所アサートしており、これを「抽出で表示を変えていない」ことの
回帰網として使うため。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 2: 言語の解決ロジック（フレームワーク非依存）

`src/lib/theme.ts` と同じ位置づけの、React に依存しない小さな中核を作る。プリペイントの bootstrap と React 側の両方がここを使う。

**Files:**
- Create: `apps/electron/src/lib/language.ts`
- Create: `apps/electron/src/lib/__tests__/language.test.ts`

**Interfaces:**
- Consumes: Task 1 の `initI18n`、`SupportedLanguage`
- Produces:
  - `type LanguagePreference = 'en' | 'ja' | 'system'`
  - `systemLanguage(): SupportedLanguage`
  - `resolveLanguage(pref: LanguagePreference): SupportedLanguage`
  - `readPersistedLanguagePreference(): LanguagePreference`
  - `bootstrapLanguage(): void`

- [ ] **Step 1: 失敗するテストを書く**

`apps/electron/src/lib/__tests__/language.test.ts`:

```ts
/**
 * Language core — the framework-free resolution shared by the pre-paint
 * bootstrap and the React reconciler. Mirrors src/lib/theme.ts.
 */

import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest'
import {
  resolveLanguage,
  systemLanguage,
  readPersistedLanguagePreference,
  bootstrapLanguage
} from '../language'
import { UI_STORE_KEY } from '../theme'
import i18n from '@/i18n'

/** Point navigator.language at a fixed value for one assertion. */
function withNavigatorLanguage(value: string, run: () => void): void {
  const spy = vi.spyOn(navigator, 'language', 'get').mockReturnValue(value)
  try {
    run()
  } finally {
    spy.mockRestore()
  }
}

beforeEach(() => {
  localStorage.clear()
})

afterEach(() => {
  void i18n.changeLanguage('en')
})

describe('systemLanguage', () => {
  it("returns 'ja' for any Japanese locale tag", () => {
    withNavigatorLanguage('ja', () => expect(systemLanguage()).toBe('ja'))
    withNavigatorLanguage('ja-JP', () => expect(systemLanguage()).toBe('ja'))
  })

  it("returns 'en' for every other locale", () => {
    withNavigatorLanguage('en-US', () => expect(systemLanguage()).toBe('en'))
    withNavigatorLanguage('de-DE', () => expect(systemLanguage()).toBe('en'))
    withNavigatorLanguage('', () => expect(systemLanguage()).toBe('en'))
  })
})

describe('resolveLanguage', () => {
  it('passes explicit preferences straight through', () => {
    expect(resolveLanguage('en')).toBe('en')
    expect(resolveLanguage('ja')).toBe('ja')
  })

  it("resolves 'system' through navigator.language", () => {
    withNavigatorLanguage('ja-JP', () => expect(resolveLanguage('system')).toBe('ja'))
    withNavigatorLanguage('en-GB', () => expect(resolveLanguage('system')).toBe('en'))
  })
})

describe('readPersistedLanguagePreference', () => {
  it("defaults to 'system' when nothing is stored", () => {
    expect(readPersistedLanguagePreference()).toBe('system')
  })

  it('reads the preference the UI store persisted', () => {
    localStorage.setItem(UI_STORE_KEY, JSON.stringify({ state: { language: 'ja' } }))
    expect(readPersistedLanguagePreference()).toBe('ja')
  })

  it("defaults to 'system' for unparseable or unknown values", () => {
    localStorage.setItem(UI_STORE_KEY, 'not json')
    expect(readPersistedLanguagePreference()).toBe('system')

    localStorage.setItem(UI_STORE_KEY, JSON.stringify({ state: { language: 'fr' } }))
    expect(readPersistedLanguagePreference()).toBe('system')
  })
})

describe('bootstrapLanguage', () => {
  it('initialises i18next with the persisted preference', () => {
    localStorage.setItem(UI_STORE_KEY, JSON.stringify({ state: { language: 'ja' } }))
    bootstrapLanguage()
    expect(i18n.language).toBe('ja')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
cd apps/electron && npx vitest run src/lib/__tests__/language.test.ts
```

期待: `Failed to resolve import "../language"` で FAIL。

- [ ] **Step 3: 実装する**

`apps/electron/src/lib/language.ts`:

```ts
/**
 * Language resolution — the small, framework-free core shared between the
 * pre-paint bootstrap (main.tsx, runs before React) and the React reconciler
 * (useLanguage). Mirrors src/lib/theme.ts so the two preferences behave alike.
 *
 * 'system' resolves through navigator.language rather than an IPC call to
 * app.getLocale(). The values agree — Electron sets Chromium's locale from the
 * same source — but navigator.language is readable synchronously, and the
 * pre-paint path cannot await an IPC round trip.
 */

import { initI18n, type SupportedLanguage } from '@/i18n'
import { UI_STORE_KEY } from './theme'

export type LanguagePreference = SupportedLanguage | 'system'

/** The OS/app locale collapsed to a supported language. */
export function systemLanguage(): SupportedLanguage {
  if (typeof navigator === 'undefined') return 'en'
  return navigator.language?.toLowerCase().startsWith('ja') ? 'ja' : 'en'
}

/** Collapse a preference to the concrete language to render. */
export function resolveLanguage(pref: LanguagePreference): SupportedLanguage {
  return pref === 'system' ? systemLanguage() : pref
}

/**
 * Best-effort read of the persisted language preference straight from
 * localStorage — used pre-paint, before the Zustand store has hydrated.
 * Returns 'system' when absent or unparseable.
 */
export function readPersistedLanguagePreference(): LanguagePreference {
  if (typeof localStorage === 'undefined') return 'system'
  try {
    const raw = localStorage.getItem(UI_STORE_KEY)
    if (!raw) return 'system'
    const parsed = JSON.parse(raw)
    const pref = parsed?.state?.language
    return pref === 'en' || pref === 'ja' || pref === 'system' ? pref : 'system'
  } catch {
    return 'system'
  }
}

/**
 * Initialise i18next with the persisted (or system-default) language. Call this
 * once, synchronously, before React renders so there is no flash of English.
 */
export function bootstrapLanguage(): void {
  initI18n(resolveLanguage(readPersistedLanguagePreference()))
}
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
cd apps/electron && npx vitest run src/lib/__tests__/language.test.ts
```

期待: 8 件 PASS。

- [ ] **Step 5: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/lib/language.ts apps/electron/src/lib/__tests__/language.test.ts
git commit -F - <<'EOF'
feat: 言語解決のフレームワーク非依存な中核を追加する

lib/theme.ts と同じ形にした。プリペイントの bootstrap と React 側の
両方が同じ解決規則を使うため、「system が何に落ちるか」を一箇所に
定義する必要がある。

system の解決は navigator.language を読む。app.getLocale() の IPC は
非同期でプリペイントに間に合わないうえ、Electron は Chromium の
ロケールを同じソースから設定するので値は一致する。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 3: 設定への言語項目の追加（main プロセス側）

**Files:**
- Modify: `apps/electron/electron/main/services/config.ts:175-182`（`AppConfig.ui` の型）と `:264-271`（`DEFAULT_CONFIG.ui`）

**Interfaces:**
- Consumes: なし
- Produces: `AppConfig['ui']['language']: 'system' | 'en' | 'ja'`

- [ ] **Step 1: 型に追加する**

`apps/electron/electron/main/services/config.ts` の `ui` の型宣言を次のように変える:

```ts
  ui: {
    theme: 'light' | 'dark' | 'system'
    // UI display language. 'system' follows the OS locale. Absent in configs
    // written before i18n existed, which resolve to 'system' via DEFAULT_CONFIG.
    language: 'system' | 'en' | 'ja'
    defaultView: 'week' | 'month'
    startOfWeek: number
    calendarView: 'day' | 'workweek' | 'week' | 'month'
    hideEmptyMeetings: boolean
    showListView: boolean
  }
```

- [ ] **Step 2: 既定値に追加する**

同ファイルの `DEFAULT_CONFIG.ui` を次のように変える:

```ts
  ui: {
    theme: 'system',
    language: 'system',
    defaultView: 'week',
    startOfWeek: 1, // Monday
    calendarView: 'week',
    hideEmptyMeetings: true,
    showListView: false
  }
```

- [ ] **Step 3: 型チェックを通す**

```bash
cd apps/electron && npm run typecheck
```

期待: EXIT 0。既定値をマージする既存の仕組みが `language` を補うため、古い `config.json` のマイグレーションは要らない。

- [ ] **Step 4: 設定のテストが通ることを確認する**

```bash
cd apps/electron && npx vitest run electron/main/services/__tests__ --project main-db
```

期待: 既存の赤 2 件（`retrieval-orchestrator.test.ts`）以外すべて PASS。

- [ ] **Step 5: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/electron/main/services/config.ts
git commit -F - <<'EOF'
feat: AppConfig.ui に language を追加する

既定値は 'system'。日本語環境の Windows では自動的に日本語になり、
英語環境では英語のままになる。'ja' を既定にすると英語話者の環境まで
日本語にしてしまう。

DEFAULT_CONFIG とのマージで補われるため、既存 config.json の
マイグレーションは不要。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 4: UI ストアへの言語の追加

**Files:**
- Modify: `apps/electron/src/types/stores.ts:240-244`
- Modify: `apps/electron/src/store/ui/useUIStore.ts`（既定値・アクション・`partialize`・セレクタ）
- Modify: `apps/electron/src/store/__tests__/useUIStore.test.ts`（テストを追加）

**Interfaces:**
- Consumes: Task 2 の `LanguagePreference`
- Produces:
  - `UIStore['language']: LanguagePreference`
  - `UIStore['setLanguage']: (language: LanguagePreference) => void`
  - `useLanguagePreference()` セレクタ

- [ ] **Step 1: 失敗するテストを書く**

`apps/electron/src/store/__tests__/useUIStore.test.ts` に追加する（既存の `setTheme` のテストが 182 行目付近にあるので、その近くに置く）:

```ts
  it("defaults the language preference to 'system' and persists an explicit choice", () => {
    const store = useUIStore.getState()
    expect(store.language).toBe('system')

    store.setLanguage('ja')
    expect(useUIStore.getState().language).toBe('ja')

    store.setLanguage('system')
    expect(useUIStore.getState().language).toBe('system')
  })
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
cd apps/electron && npx vitest run src/store/__tests__/useUIStore.test.ts
```

期待: `store.setLanguage is not a function` で FAIL。

- [ ] **Step 3: 型を追加する**

`apps/electron/src/types/stores.ts` の 240 行目付近、`theme` の隣に追加する:

```ts
  theme: ThemePreference
  setTheme: (theme: ThemePreference) => void
  language: LanguagePreference
  setLanguage: (language: LanguagePreference) => void
```

同ファイルの 244 行目付近、`ThemePreference` の隣に追加する:

```ts
export type ThemePreference = 'light' | 'dark' | 'system'
export type LanguagePreference = 'en' | 'ja' | 'system'
```

- [ ] **Step 4: ストアに追加する**

`apps/electron/src/store/ui/useUIStore.ts` の 75 行目付近、`theme: 'system',` の直後に既定値を追加する:

```ts
  // Theme preference — defaults to following the OS.
  theme: 'system',

  // UI language preference — defaults to following the OS locale.
  language: 'system',
```

225 行目付近、`setTheme` の直後にアクションを追加する:

```ts
  // Theme actions — the applied `dark` class is reconciled by useTheme().
  setTheme: (theme) => {
    set({ theme })
  },

  // Language actions — the active i18next language is reconciled by useLanguage().
  setLanguage: (language) => {
    set({ language })
  },
```

242 行目付近、`partialize` の `theme` の直後に追加する:

```ts
        theme: state.theme, // persisted: user preference (also read pre-paint in main.tsx)
        language: state.language, // persisted: user preference (also read pre-paint in main.tsx)
```

293 行目付近、`useThemePreference` の隣にセレクタを追加する:

```ts
export const useLanguagePreference = () => useUIStore((s) => s.language)
```

- [ ] **Step 5: テストが通ることを確認する**

```bash
cd apps/electron && npx vitest run src/store/__tests__/useUIStore.test.ts && npm run typecheck
```

期待: 全 PASS、typecheck は EXIT 0。

- [ ] **Step 6: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/types/stores.ts apps/electron/src/store/ui/useUIStore.ts \
        apps/electron/src/store/__tests__/useUIStore.test.ts
git commit -F - <<'EOF'
feat: UI ストアに言語設定を追加する

theme と同じ扱いにし、partialize の対象に明示的に加えた。
プリペイントの bootstrapLanguage() が localStorage から直接読むため、
永続化されていないと起動時に言語が飛ぶ。

追加先は正本の store/ui/useUIStore.ts。store/useUIStore.ts は
後方互換のための再エクスポートであり、そちらには手を入れない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 5: React 側の整合フックと起動時の配線

**Files:**
- Create: `apps/electron/src/hooks/useLanguage.ts`
- Create: `apps/electron/src/hooks/__tests__/useLanguage.test.tsx`
- Modify: `apps/electron/src/main.tsx`
- Modify: `apps/electron/src/App.tsx`

**Interfaces:**
- Consumes: Task 2 の `resolveLanguage` / `bootstrapLanguage`、Task 4 の `useUIStore.language` / `setLanguage`、`useConfigStore.updateConfig`
- Produces: `useLanguage(): { language: LanguagePreference; resolvedLanguage: SupportedLanguage; setLanguage: (l: LanguagePreference) => void }`

- [ ] **Step 1: 失敗するテストを書く**

`apps/electron/src/hooks/__tests__/useLanguage.test.tsx`:

```tsx
/**
 * useLanguage — reconciles the persisted language preference with i18next and
 * mirrors the choice into config.json. useTheme has no hook-level test, so this
 * is a new one rather than a copy of an existing pattern.
 */

import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest'
import { renderHook, act, waitFor } from '@testing-library/react'
import { useLanguage } from '../useLanguage'
import { useUIStore } from '@/store/ui/useUIStore'
import { useConfigStore } from '@/store/domain/useConfigStore'
import i18n from '@/i18n'

beforeEach(() => {
  useUIStore.setState({ language: 'system' })
})

afterEach(() => {
  void i18n.changeLanguage('en')
  vi.restoreAllMocks()
})

describe('useLanguage', () => {
  it('applies the preference to i18next', async () => {
    const { result } = renderHook(() => useLanguage())

    act(() => result.current.setLanguage('ja'))

    await waitFor(() => expect(i18n.language).toBe('ja'))
    expect(result.current.language).toBe('ja')
    expect(result.current.resolvedLanguage).toBe('ja')
  })

  it('mirrors the choice into config.ui.language', async () => {
    const updateConfig = vi.fn().mockResolvedValue(undefined)
    useConfigStore.setState({ updateConfig } as never)

    const { result } = renderHook(() => useLanguage())
    act(() => result.current.setLanguage('ja'))

    await waitFor(() => expect(updateConfig).toHaveBeenCalledWith('ui', { language: 'ja' }))
  })

  it('survives a failing config mirror', async () => {
    const updateConfig = vi.fn().mockRejectedValue(new Error('disk full'))
    useConfigStore.setState({ updateConfig } as never)

    const { result } = renderHook(() => useLanguage())
    act(() => result.current.setLanguage('ja'))

    // localStorage remains the source of truth; the rejection must not surface.
    await waitFor(() => expect(useUIStore.getState().language).toBe('ja'))
  })
})
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
cd apps/electron && npx vitest run src/hooks/__tests__/useLanguage.test.tsx
```

期待: `Failed to resolve import "../useLanguage"` で FAIL。

- [ ] **Step 3: フックを実装する**

`apps/electron/src/hooks/useLanguage.ts`:

```ts
/**
 * useLanguage — reconciles the persisted language preference with i18next.
 *
 * Responsibilities:
 *  - switch i18next whenever the preference changes;
 *  - mirror the preference into the app config (config.ui.language) best-effort,
 *    so the choice is durable beyond localStorage.
 *
 * The pre-paint bootstrap in main.tsx has already initialised i18next from
 * localStorage before React mounts, so this hook only keeps things in sync — it
 * never causes a flash of English.
 *
 * Mirrors hooks/useTheme.ts.
 */

import { useCallback, useEffect } from 'react'
import { useUIStore } from '@/store/ui/useUIStore'
import { useConfigStore } from '@/store/domain/useConfigStore'
import { resolveLanguage, type LanguagePreference } from '@/lib/language'
import i18n, { type SupportedLanguage } from '@/i18n'

export interface UseLanguageResult {
  /** The user's preference: 'en' | 'ja' | 'system'. */
  language: LanguagePreference
  /** The concrete language currently rendered. */
  resolvedLanguage: SupportedLanguage
  /** Set the preference (persists + applies + mirrors to config). */
  setLanguage: (language: LanguagePreference) => void
}

export function useLanguage(): UseLanguageResult {
  const language = useUIStore((s) => s.language)
  const setLanguagePref = useUIStore((s) => s.setLanguage)
  const configReady = useConfigStore((s) => s.configReady)
  const configLanguage = useConfigStore((s) => s.config?.ui?.language)

  // Apply on preference change.
  useEffect(() => {
    const resolved = resolveLanguage(language)
    if (i18n.language !== resolved) void i18n.changeLanguage(resolved)
  }, [language])

  // One-time adoption: if the user has never picked a language in THIS renderer
  // (localStorage still 'system') but config.json carries an explicit choice
  // from a prior session, adopt it. Avoids clobbering a fresh localStorage pick.
  useEffect(() => {
    if (!configReady) return
    if (language !== 'system') return
    if (configLanguage === 'en' || configLanguage === 'ja') {
      setLanguagePref(configLanguage)
    }
    // Only react to config readiness; `language` intentionally omitted so a later
    // manual switch back to 'system' isn't immediately overridden by config.
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [configReady, configLanguage])

  const setLanguage = useCallback(
    (next: LanguagePreference) => {
      setLanguagePref(next)
      const resolved = resolveLanguage(next)
      if (i18n.language !== resolved) void i18n.changeLanguage(resolved)
      // Mirror to config, best-effort — a failure just leaves localStorage as
      // the source of truth. updateConfig already restores on error internally.
      void useConfigStore
        .getState()
        .updateConfig('ui', { language: next })
        .catch(() => {})
    },
    [setLanguagePref]
  )

  return { language, resolvedLanguage: resolveLanguage(language), setLanguage }
}
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
cd apps/electron && npx vitest run src/hooks/__tests__/useLanguage.test.tsx
```

期待: 3 件 PASS。

- [ ] **Step 5: 起動時に配線する**

`apps/electron/src/main.tsx` の import に追加する:

```ts
import { bootstrapLanguage } from './lib/language'
```

`bootstrapTheme()` の呼び出しの直後に追加する:

```ts
// Apply the persisted (or OS-default) theme BEFORE React mounts, so there is no
// light/dark flash on load. useTheme() then keeps it reconciled at runtime.
bootstrapTheme()

// Same contract for the UI language: initialise i18next from localStorage before
// React mounts so the first paint is already in the right language rather than
// flashing English. useLanguage() keeps it reconciled at runtime.
bootstrapLanguage()
```

`apps/electron/src/App.tsx` の import に追加する:

```ts
import { useLanguage } from '@/hooks/useLanguage'
```

105 行目の `useTheme()` の直後に追加する:

```ts
  useTheme()
  useLanguage()
```

- [ ] **Step 6: 全テストが基準どおりであることを確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck EXIT 0。既知の赤 3 件以外はすべて緑。

- [ ] **Step 7: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/hooks/useLanguage.ts \
        apps/electron/src/hooks/__tests__/useLanguage.test.tsx \
        apps/electron/src/main.tsx apps/electron/src/App.tsx
git commit -F - <<'EOF'
feat: 言語設定を React 側と起動時に配線する

useTheme と同じ二段構えにした。main.tsx のプリペイントで
localStorage から i18next を初期化し、useLanguage が実行時の
整合とconfig.json へのミラーを担う。これで起動時に英語が
一瞬見えてから切り替わる挙動を避けられる。

config への書き込み失敗は握り潰す。失敗しても localStorage が
正であり続けるため、言語切替そのものを壊すべきではない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 6: 設定画面の言語セレクタ

**Files:**
- Modify: `apps/electron/src/pages/Settings.tsx`（724 行目付近、Assistant カードの直前に新しい `<Card>` を挿入）
- Create: `apps/electron/src/pages/__tests__/Settings.language.test.tsx`

**Interfaces:**
- Consumes: Task 5 の `useLanguage`
- Produces: なし（UI のみ）

この時点ではまだ UI 文字列を翻訳していないため、セレクタのラベル自体は英語で書く。Task 8 で `settings` ネームスペースへ移す。

- [ ] **Step 1: 失敗するテストを書く**

`apps/electron/src/pages/__tests__/Settings.language.test.tsx`:

```tsx
/**
 * The language selector is the only UI control for a UI-language preference in
 * the app. There is no theme selector to sit beside — useTheme() is a reconciler
 * with no control wired to it — so this is built from scratch.
 */

import { describe, it, expect, vi, afterEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { useUIStore } from '@/store/ui/useUIStore'
import i18n from '@/i18n'

const setLanguage = vi.fn()

vi.mock('@/hooks/useLanguage', () => ({
  useLanguage: () => ({
    language: useUIStore.getState().language,
    resolvedLanguage: 'en',
    setLanguage
  })
}))

afterEach(() => {
  void i18n.changeLanguage('en')
  useUIStore.setState({ language: 'system' })
  vi.clearAllMocks()
})

describe('Settings — language selector', () => {
  it('offers the three preferences and reports the active one', async () => {
    const { LanguageSettingsCard } = await import('../Settings')
    render(<LanguageSettingsCard />)

    const group = screen.getByRole('group', { name: 'Display language' })
    expect(group).toBeInTheDocument()

    expect(screen.getByRole('button', { name: 'System' })).toHaveAttribute('aria-pressed', 'true')
    expect(screen.getByRole('button', { name: 'English' })).toHaveAttribute('aria-pressed', 'false')
    expect(screen.getByRole('button', { name: '日本語' })).toHaveAttribute('aria-pressed', 'false')
  })

  it('sets the preference when a language is picked', async () => {
    const { LanguageSettingsCard } = await import('../Settings')
    render(<LanguageSettingsCard />)

    await userEvent.click(screen.getByRole('button', { name: '日本語' }))

    await waitFor(() => expect(setLanguage).toHaveBeenCalledWith('ja'))
  })
})
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
cd apps/electron && npx vitest run src/pages/__tests__/Settings.language.test.tsx
```

期待: `LanguageSettingsCard is not a function` で FAIL。

- [ ] **Step 3: カードを実装する**

`apps/electron/src/pages/Settings.tsx` の import に追加する:

```ts
import { useLanguage } from '@/hooks/useLanguage'
import type { LanguagePreference } from '@/lib/language'
```

同ファイルの `Settings` コンポーネント定義の**外側**（ファイル上部の定数定義の後）に、テストから単体で描画できるよう export 付きで追加する:

```tsx
/** The three display-language choices, in the order they appear. */
const LANGUAGE_OPTIONS: ReadonlyArray<{ value: LanguagePreference; label: string }> = [
  { value: 'system', label: 'System' },
  { value: 'en', label: 'English' },
  { value: 'ja', label: '日本語' }
]

/**
 * Display-language picker. Uses the same segmented-button shape as the
 * Assistant card's placement/position controls so the Settings page has one
 * interaction idiom rather than two.
 */
export function LanguageSettingsCard(): React.ReactElement {
  const { language, setLanguage } = useLanguage()

  return (
    <Card>
      <CardHeader>
        <CardTitle>Appearance</CardTitle>
        <CardDescription>Choose the language the interface is displayed in</CardDescription>
      </CardHeader>
      <CardContent className="space-y-5">
        <div className="space-y-2">
          <span className="text-sm font-medium">Display language</span>
          <div
            role="group"
            aria-label="Display language"
            className="inline-flex rounded-lg border border-input bg-muted/40 p-0.5"
          >
            {LANGUAGE_OPTIONS.map((option) => (
              <button
                key={option.value}
                type="button"
                aria-pressed={language === option.value}
                onClick={() => setLanguage(option.value)}
                className={cn(
                  'flex items-center gap-1.5 rounded-md px-3 py-1.5 text-sm transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring',
                  language === option.value
                    ? 'bg-background font-medium text-foreground shadow-sm'
                    : 'text-muted-foreground hover:text-foreground'
                )}
              >
                {option.label}
              </button>
            ))}
          </div>
          <p className="text-xs text-muted-foreground">
            System follows your operating system&apos;s language. Changes apply immediately.
          </p>
        </div>
      </CardContent>
    </Card>
  )
}
```

`Settings` コンポーネントの中、725 行目付近の Assistant の `<Card>` の直前に挿入する:

```tsx
          <LanguageSettingsCard />

          <Card>
            <CardHeader>
              <CardTitle>Assistant</CardTitle>
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
cd apps/electron && npx vitest run src/pages/__tests__/Settings.language.test.tsx
```

期待: 2 件 PASS。

- [ ] **Step 5: 既存の Settings テストが壊れていないことを確認する**

```bash
cd apps/electron && npx vitest run src/pages/__tests__/Settings.test.tsx
```

期待: 全 PASS。新しいカードを足しただけなので既存のアサートには影響しないはずである。**赤くなったら、追加したカードが既存のクエリと衝突していないか確認する。**

- [ ] **Step 6: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/pages/Settings.tsx \
        apps/electron/src/pages/__tests__/Settings.language.test.tsx
git commit -F - <<'EOF'
feat: 設定画面に表示言語のセレクタを追加する

アプリには元々テーマセレクタが存在しない（useTheme は整合用に
呼ばれるだけで、setTheme を叩く UI が無い）。隣に置けるものが
無いため、Appearance カードを新設した。

実装は Assistant カードのセグメントボタンに合わせた。設定画面の中で
操作方法を2種類に分けないため。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 7: layout ネームスペースの英語抽出

ここから文字列の移行に入る。**このタスクの成否が以降すべての前提になる**ため、最小のネームスペースで手順を確立する。

**Files:**
- Modify: `apps/electron/src/i18n/index.ts`（`layout` カタログの import と resources への登録）
- Create: `apps/electron/src/i18n/locales/en/layout.json`
- Create: `apps/electron/src/i18n/locales/ja/layout.json`（この時点では `{}`）
- Modify: 対象 10 ファイル
  - `src/components/layout/TitleBar.tsx`
  - `src/components/layout/Layout.tsx`
  - `src/components/layout/UserMenu.tsx`
  - `src/components/layout/OperationsPanel.tsx`
  - `src/components/layout/NotificationsButton.tsx`
  - `src/components/layout/ActivityLogButton.tsx`
  - `src/components/layout/Brand.tsx`
  - `src/components/ui/toaster.tsx`
  - `src/components/ui/dialog.tsx`
  - `src/App.tsx`

**Interfaces:**
- Consumes: Task 1 の `initI18n`、`NAMESPACES`
- Produces: `layout` ネームスペースのキー群。以降のタスクは `t('layout:...')` を参照できる

対象は約 87 文字列（JSX テキスト 29・属性 33・残りはトーストとエラー文言）。

- [ ] **Step 1: 対象ファイルの英語文字列を洗い出す**

```bash
cd apps/electron/src
grep -nE ">[[:space:]]*[A-Z][A-Za-z0-9 ,.'!?&/:%()-]{2,}[[:space:]]*<" \
  components/layout/*.tsx components/ui/toaster.tsx components/ui/dialog.tsx App.tsx
grep -nE '(placeholder|title|aria-label|label|description|tooltip)="[^"]{3,}"' \
  components/layout/*.tsx components/ui/toaster.tsx components/ui/dialog.tsx App.tsx
```

出力をそのまま作業リストにする。**表示されない文字列（`className`、`data-*`、`key`、ログ出力、`console.*` の引数）は対象外。**

- [ ] **Step 2: カタログを書く**

`apps/electron/src/i18n/locales/en/layout.json` に、洗い出した文字列を**一字一句そのまま**入れる。キーは `<component>.<slug>` 形式。例:

```json
{
  "titleBar.minimize": "Minimize",
  "titleBar.maximize": "Maximize",
  "titleBar.close": "Close",
  "operations.title": "Operations",
  "operations.empty": "No operations running",
  "notifications.label": "Notifications",
  "activityLog.label": "Activity log",
  "assistant.title": "Assistant",
  "assistant.loading": "Loading assistant..."
}
```

`apps/electron/src/i18n/locales/ja/layout.json`:

```json
{}
```

- [ ] **Step 3: カタログを登録する**

`apps/electron/src/i18n/index.ts` の import に追加する:

```ts
import enLayout from './locales/en/layout.json'
import jaLayout from './locales/ja/layout.json'
```

`resources` を次のように変える:

```ts
const resources = {
  en: { common: enCommon, layout: enLayout },
  ja: { common: jaCommon, layout: jaLayout }
}
```

- [ ] **Step 4: コンポーネントを置き換える**

各ファイルで `useTranslation` を使う。例（`App.tsx` の `FloatingAssistant`）:

置換前:

```tsx
      <FloatingAssistant title="Assistant">
        <Suspense fallback={<LoadingSpinner message="Loading assistant..." />}>
```

置換後:

```tsx
      <FloatingAssistant title={t('layout:assistant.title')}>
        <Suspense fallback={<LoadingSpinner message={t('layout:assistant.loading')} />}>
```

コンポーネントの先頭に追加する:

```tsx
  const { t } = useTranslation()
```

import に追加する:

```tsx
import { useTranslation } from 'react-i18next'
```

**複数形を含む文字列は `t()` の `count` オプションに畳む。** 例:

置換前:

```tsx
{stats.total} recording{stats.total !== 1 ? 's' : ''}
```

置換後:

```tsx
{t('layout:stats.recordings', { count: stats.total })}
```

カタログ側:

```json
{
  "stats.recordings_one": "{{count}} recording",
  "stats.recordings_other": "{{count}} recordings"
}
```

**文中にマークアップを含む箇所は `<Trans>` を使い、キーを 1 個に保つ。** 文を分割して `t()` を並べてはいけない。日本語では語順が変わって破綻する。

- [ ] **Step 5: 英語表示が 1 文字も変わっていないことを確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck EXIT 0。テストは既知の赤 3 件のみ。

**`ByText()` のアサートが落ちたら、それは抽出のミスである。** カタログの英語がコンポーネントの元の文字列と 1 文字でも違えば落ちる。落ちたテストが期待している文字列とカタログを突き合わせて直す。テスト側を書き換えて通してはいけない。

- [ ] **Step 6: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/i18n apps/electron/src/components/layout \
        apps/electron/src/components/ui/toaster.tsx \
        apps/electron/src/components/ui/dialog.tsx apps/electron/src/App.tsx
git commit -F - <<'EOF'
refactor: 共通レイアウトの英語文字列をカタログへ抽出する

表示は一切変えていない。英語カタログはコンポーネントにあった
文字列をそのまま写したもので、既存テストの ByText アサートが
全て通ることがその証明になる。

日本語の翻訳は別コミットで入れる。抽出と翻訳を混ぜると、
テストが落ちたときに抽出漏れか翻訳ミスかを切り分けられない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 8: settings ネームスペースの英語抽出

**Files:**
- Modify: `apps/electron/src/i18n/index.ts`
- Create: `apps/electron/src/i18n/locales/en/settings.json` / `ja/settings.json`
- Modify: 対象 4 ファイル
  - `src/pages/Settings.tsx`（35 テキスト + 31 属性）
  - `src/components/settings/AIBrainsSettings.tsx`（7 + 2）
  - `src/components/settings/ConnectorsSettings.tsx`（3 + 2）
  - `src/components/settings/FeaturesSettings.tsx`（2 + 1）

**Interfaces:**
- Consumes: Task 7 で確立した抽出手順
- Produces: `settings` ネームスペースのキー群

約 120 文字列。Task 6 で追加した `LanguageSettingsCard` の英語文字列（`Appearance` / `Display language` / `System` / `English` / `Changes apply immediately.` など）もここで移す。ただし **`日本語` という選択肢のラベルは翻訳しない**。言語の名前はその言語自身で表記するのが慣行であり、英語表示でも `日本語` のままにする。

- [ ] **Step 1: 対象ファイルの英語文字列を洗い出す**

```bash
cd apps/electron/src
grep -nE ">[[:space:]]*[A-Z][A-Za-z0-9 ,.'!?&/:%()-]{2,}[[:space:]]*<" \
  pages/Settings.tsx components/settings/*.tsx
grep -nE '(placeholder|title|aria-label|label|description|tooltip)="[^"]{3,}"' \
  pages/Settings.tsx components/settings/*.tsx
```

- [ ] **Step 2: カタログを書いて登録する**

`apps/electron/src/i18n/locales/en/settings.json` に一字一句そのまま入れる。例:

```json
{
  "appearance.title": "Appearance",
  "appearance.description": "Choose the language the interface is displayed in",
  "language.label": "Display language",
  "language.system": "System",
  "language.english": "English",
  "language.hint": "System follows your operating system's language. Changes apply immediately.",
  "assistant.title": "Assistant",
  "assistant.description": "Choose how the AI assistant appears while you work",
  "assistant.placement": "Chat placement",
  "calendar.title": "Calendar",
  "calendar.description": "Configure calendar sync from Outlook",
  "transcription.title": "Transcription",
  "transcription.description": "Choose cloud Gemini or local ASR for meeting transcripts",
  "loadError.title": "Failed to Load Settings"
}
```

`ja/settings.json` は `{}`。`src/i18n/index.ts` の `resources` に `settings: enSettings` / `settings: jaSettings` を追加する。

- [ ] **Step 3: コンポーネントを置き換える**

`LANGUAGE_OPTIONS` は `t` を使えるようフックの中へ移す:

```tsx
export function LanguageSettingsCard(): React.ReactElement {
  const { t } = useTranslation()
  const { language, setLanguage } = useLanguage()

  const options: ReadonlyArray<{ value: LanguagePreference; label: string }> = [
    { value: 'system', label: t('settings:language.system') },
    { value: 'en', label: t('settings:language.english') },
    // A language's own name is written in that language by convention, so this
    // one is intentionally not translated.
    { value: 'ja', label: '日本語' }
  ]
  // ...残りは options を使うよう置き換える
}
```

- [ ] **Step 4: 確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck EXIT 0、既知の赤 3 件のみ。Task 6 で書いた `Settings.language.test.tsx` も通り続ける（`System` / `English` / `日本語` の表示は変わらないため）。

- [ ] **Step 5: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/i18n apps/electron/src/pages/Settings.tsx \
        apps/electron/src/components/settings
git commit -F - <<'EOF'
refactor: 設定画面の英語文字列をカタログへ抽出する

言語選択の「日本語」だけは翻訳対象から外した。言語名は
その言語自身で表記するのが慣行であり、英語表示のときも
日本語のままにする。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 9: device ネームスペースの英語抽出

**Files:**
- Modify: `apps/electron/src/i18n/index.ts`
- Create: `apps/electron/src/i18n/locales/en/device.json` / `ja/device.json`
- Modify:
  - `src/pages/Device.tsx`（8 テキスト + 2 属性）
  - `src/components/DeviceFileList.tsx`（2 + 3）
  - `src/components/LiveRecordingCard.tsx`（5 + 1）

**Interfaces:**
- Consumes: Task 7 で確立した抽出手順
- Produces: `device` ネームスペースのキー群

約 74 文字列。

- [ ] **Step 1: 洗い出す**

```bash
cd apps/electron/src
grep -nE ">[[:space:]]*[A-Z][A-Za-z0-9 ,.'!?&/:%()-]{2,}[[:space:]]*<" \
  pages/Device.tsx components/DeviceFileList.tsx components/LiveRecordingCard.tsx
grep -nE '(placeholder|title|aria-label|label|description|tooltip)="[^"]{3,}"' \
  pages/Device.tsx components/DeviceFileList.tsx components/LiveRecordingCard.tsx
```

- [ ] **Step 2: カタログを書いて登録する**

`en/device.json` に一字一句そのまま。`ja/device.json` は `{}`。`src/i18n/index.ts` の `resources` に登録する。

- [ ] **Step 3: コンポーネントを置き換える**

Task 7 と同じ手順。`useTranslation` を入れ、文字列を `t('device:...')` に置き換える。複数形は `count` に畳み、マークアップを含む文は `<Trans>` を使う。

- [ ] **Step 4: 確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck EXIT 0、既知の赤 3 件のみ。

- [ ] **Step 5: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/i18n apps/electron/src/pages/Device.tsx \
        apps/electron/src/components/DeviceFileList.tsx \
        apps/electron/src/components/LiveRecordingCard.tsx
git commit -F - <<'EOF'
refactor: デバイス画面の英語文字列をカタログへ抽出する

表示は変えていない。既存テストの ByText アサートが通ることが
その証明になる。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 10: today ネームスペースの英語抽出

**Files:**
- Modify: `apps/electron/src/i18n/index.ts`
- Create: `apps/electron/src/i18n/locales/en/today.json` / `ja/today.json`
- Modify: `src/pages/Today.tsx` と `src/features/today/` 配下の 4 ファイル

**Interfaces:**
- Consumes: Task 7 で確立した抽出手順
- Produces: `today` ネームスペースのキー群

約 32 文字列。`src/features/today/TodayCaptures.tsx:46` に `{captures.length} {captures.length === 1 ? 'item' : 'items'}` という複数形があるので、これを `count` に畳む練習になる。

- [ ] **Step 1: 洗い出す**

```bash
cd apps/electron/src
grep -nE ">[[:space:]]*[A-Z][A-Za-z0-9 ,.'!?&/:%()-]{2,}[[:space:]]*<" \
  pages/Today.tsx features/today/*.tsx
grep -nE '(placeholder|title|aria-label|label|description|tooltip)="[^"]{3,}"' \
  pages/Today.tsx features/today/*.tsx
```

- [ ] **Step 2: カタログを書いて登録する**

`en/today.json` の複数形は次の形にする:

```json
{
  "captures.items_one": "{{count}} item",
  "captures.items_other": "{{count}} items"
}
```

`TodayCaptures.tsx:46` の置換:

```tsx
            {t('today:captures.items', { count: captures.length })}
```

元は数値と単位が別々のノードだったが、1 つのキーに統合する。日本語では「3件」のように語順と助数詞が変わるため、分割したままでは訳せない。

- [ ] **Step 3: 確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck EXIT 0、既知の赤 3 件のみ。

**`{captures.length} {captures.length === 1 ? 'item' : 'items'}` は「数字・空白・単語」の 3 ノードだったのが 1 ノードになる。** `getByText('3 items')` のような完全一致のアサートは通るが、部分一致や正規表現のアサートが落ちる可能性がある。落ちた場合は、レンダリング結果の文字列が元と同じか確認する。同じならテスト側のクエリがノード構造に依存していたということなので、そのテストのクエリを直してよい。

- [ ] **Step 4: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/i18n apps/electron/src/pages/Today.tsx \
        apps/electron/src/features/today
git commit -F - <<'EOF'
refactor: Today 画面の英語文字列をカタログへ抽出する

「{n} item(s)」のように数値と単位が別ノードだった複数形を
キー1個に統合した。日本語では語順と助数詞が変わるため、
分割したままでは訳せない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 11: library ネームスペースの英語抽出（55 ファイル / 403 文字列）

Phase 1 の過半を占める最大の塊。1 コミットにまとめず、下のサブタスクごとにコミットする。各サブタスクの終わりに必ず `npm run test:run` を通す。

**Files:**
- Modify: `apps/electron/src/i18n/index.ts`
- Create: `apps/electron/src/i18n/locales/en/library.json` / `ja/library.json`
- Modify: `src/pages/Library.tsx` と `src/features/library/**` の 54 ファイル

**Interfaces:**
- Consumes: Task 7 で確立した抽出手順
- Produces: `library` ネームスペースのキー群

**サブタスクの分割**（文字列数の多い順、依存の少ない順）:

- [ ] **11a: リーダー系（約 75 文字列）** — `SourceReader.tsx`（30 テキスト + 33 属性）、`ArtifactReader.tsx`（10 + 2）、`ReaderSectionControls.tsx`（2 + 0）
- [ ] **11b: 一覧と行（約 60 文字列）** — `SourceRow.tsx`、`SourceRowExpanded.tsx`、`SourceCard.tsx`、`SourceDetailDrawer.tsx`（23 + 0）
- [ ] **11c: 絞り込みとヘッダ（約 45 文字列）** — `LibraryFilters.tsx`（15 + 13）、`LibraryHeader.tsx`（2 + 9）、`pages/Library.tsx`（6 + 5）
- [ ] **11d: 一括操作（約 30 文字列）** — `BulkActionsBar.tsx`、`BulkResultSummary.tsx`、`BulkProgressModal.tsx`、`MultiSelectionSummary.tsx`、`DeletePermanentDialog.tsx`
- [ ] **11e: 文字起こしと話者（約 35 文字列）** — `TranscriptViewer.tsx`、`TranscriptUpgradeButton.tsx`、`SpeakerAssignPopover.tsx`、`RecordingSplitEditor.tsx`
- [ ] **11f: 状態表示とレイアウト（約 50 文字列）** — `StatusIcon.tsx`（4 + 8）、`StatusLegend.tsx`、`EmptyState.tsx`、`TriPaneLayout.tsx`（3 + 23）、`WaveformPlayer.tsx`（8 + 6）、`AssistantPanel.tsx`
- [ ] **11g: 残り** — 上のどれにも入らないファイル。着手前に `git status` と下のコマンドで残りを洗い出す

各サブタスクの手順は Task 7 と同じ。洗い出しのコマンドは対象ファイルを差し替える:

```bash
cd apps/electron/src
grep -nE ">[[:space:]]*[A-Z][A-Za-z0-9 ,.'!?&/:%()-]{2,}[[:space:]]*<" \
  features/library/components/SourceReader.tsx features/library/components/ArtifactReader.tsx
grep -nE '(placeholder|title|aria-label|label|description|tooltip)="[^"]{3,}"' \
  features/library/components/SourceReader.tsx features/library/components/ArtifactReader.tsx
```

**11g の残り洗い出し:**

```bash
cd apps/electron/src
# まだ useTranslation を入れていない library 配下のファイル
for f in $(find features/library pages/Library.tsx -name '*.tsx' -not -path '*__tests__*'); do
  grep -q "useTranslation\|Trans" "$f" || echo "$f"
done
```

各サブタスクのコミット例（11a）:

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/i18n \
        apps/electron/src/features/library/components/SourceReader.tsx \
        apps/electron/src/features/library/components/ArtifactReader.tsx \
        apps/electron/src/features/library/components/ReaderSectionControls.tsx
git commit -F - <<'EOF'
refactor: ライブラリのリーダー系の英語文字列をカタログへ抽出する

library ネームスペースは 55 ファイル 403 文字列と大きいため、
コンポーネントの役割ごとに分けてコミットする。1 コミットに
まとめると、テストが落ちたときに原因箇所を絞り込めない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 12: 日付と時刻のロケール対応

**Files:**
- Modify: `apps/electron/src/lib/smartDate.ts`
- Modify: `apps/electron/src/lib/__tests__/smartDate.test.ts`（存在しなければ作成）
- Modify: `apps/electron/src/i18n/locales/en/common.json` / `ja/common.json`

**Interfaces:**
- Consumes: Task 1 の i18next インスタンス
- Produces: ロケールを見る `formatSmartDate` / `formatRelativeDate`

- [ ] **Step 1: 失敗するテストを書く**

`apps/electron/src/lib/__tests__/smartDate.test.ts` に追加する:

```ts
import { describe, it, expect, afterEach } from 'vitest'
import { formatSmartDate, formatRelativeDate } from '../smartDate'
import i18n from '@/i18n'

afterEach(() => {
  void i18n.changeLanguage('en')
})

describe('formatSmartDate — locale', () => {
  const when = new Date('2026-09-22T11:14:00')

  it('formats in US English by default', () => {
    expect(formatSmartDate(when, { time: false })).toBe('Sep 22, 2026')
  })

  it('formats in Japanese when the UI language is ja', async () => {
    await i18n.changeLanguage('ja')
    expect(formatSmartDate(when, { time: false })).toBe('2026年9月22日')
  })
})

describe('formatRelativeDate — locale', () => {
  const now = new Date('2026-09-22T12:00:00')
  const threeDaysAgo = new Date('2026-09-19T12:00:00')

  it('renders English relative hints', () => {
    expect(formatRelativeDate(threeDaysAgo, now)).toBe('3 days ago')
  })

  it('renders Japanese relative hints', async () => {
    await i18n.changeLanguage('ja')
    expect(formatRelativeDate(threeDaysAgo, now)).toBe('3日前')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
cd apps/electron && npx vitest run src/lib/__tests__/smartDate.test.ts
```

期待: 日本語の 2 件が FAIL（英語の 2 件は現状のまま通る）。

- [ ] **Step 3: 実装する**

`apps/electron/src/lib/smartDate.ts` の先頭に追加する:

```ts
import i18n from '@/i18n'

/**
 * The BCP 47 tag to format dates with. Derived from the active UI language
 * rather than the OS locale: a user who picked English in Settings expects
 * English dates even on a Japanese Windows.
 */
function dateLocale(): string {
  return i18n.language === 'ja' ? 'ja-JP' : 'en-US'
}
```

`formatSmartDate` の中の `'en-US'` を `dateLocale()` に置き換え、フォールバック文言をカタログから引く:

```ts
export function formatSmartDate(
  value: Date | string | number | null | undefined,
  opts: { time?: boolean; fallback?: string } = {}
): string {
  const d = toDate(value)
  if (!d) return opts.fallback ?? i18n.t('common:date.unknown')
  const locale = dateLocale()
  const date = d.toLocaleDateString(locale, { month: 'short', day: 'numeric', year: 'numeric' })
  if (opts.time === false) return date
  const time = d.toLocaleTimeString(locale, { hour: 'numeric', minute: '2-digit' })
  return `${date} · ${time}`
}
```

相対表記は、**英語の分岐をそのまま残したうえで、日本語のときだけ別経路に送る。**

既存の英語表記は `just now` / `5 min ago` / `3 h ago` / `3 days ago` / `2 wks ago` / `5 mo ago` / `2 yr ago` という独自の短縮形である。`Intl.RelativeTimeFormat` の既定はこれとほぼ全単位で食い違う（`5 minutes ago` / `3 hours ago` / `2 weeks ago` / `5 months ago` / `2 years ago`）。英語の分岐を `Intl` に置き換えると、**日本語化とは無関係に英語の表示を変えてしまう**。それは本設計の根幹（抽出は表示を変えない）に反するうえ、依頼されていない仕様変更である。

`formatRelativeDate` の既存の本体は一切変えず、関数の冒頭に日本語向けの早期リターンを足す:

```ts
/** Thresholds in seconds, largest first, with the Intl unit to report them in. */
const RELATIVE_UNITS: ReadonlyArray<[Intl.RelativeTimeFormatUnit, number]> = [
  ['year', 60 * 60 * 24 * 365],
  ['month', 60 * 60 * 24 * 30],
  ['week', 60 * 60 * 24 * 7],
  ['day', 60 * 60 * 24],
  ['hour', 60 * 60],
  ['minute', 60]
]

/**
 * Japanese relative hint via Intl. Only reached when the UI language is ja, so
 * the English branch below keeps producing its established short forms
 * ("5 min ago", "2 wks ago") byte-for-byte.
 */
function formatRelativeJa(d: Date, now: Date): string {
  const deltaSeconds = (d.getTime() - now.getTime()) / 1000
  const abs = Math.abs(deltaSeconds)
  if (abs < 45) return i18n.t('common:date.justNow')

  const rtf = new Intl.RelativeTimeFormat('ja-JP', { numeric: 'always' })
  for (const [unit, seconds] of RELATIVE_UNITS) {
    if (abs >= seconds) return rtf.format(Math.round(deltaSeconds / seconds), unit)
  }
  return rtf.format(Math.round(deltaSeconds / 60), 'minute')
}
```

`formatRelativeDate` への追加は、`toDate` の直後の 2 行だけである:

```ts
export function formatRelativeDate(
  value: Date | string | number | null | undefined,
  now: Date = new Date()
): string | null {
  const d = toDate(value)
  if (!d) return null

  // Japanese takes the Intl path; English keeps its own short forms unchanged.
  if (i18n.language === 'ja') return formatRelativeJa(d, now)

  const diffMs = now.getTime() - d.getTime()
  // ...ここから下は既存のまま、1 行も変えない
```

`numeric: 'always'` を使うのは、既定の `'auto'` が 1 日前を「昨日」に置き換えてしまうためである。英語側は日数をそのまま出しているので、日本語側も「1日前」に揃える。

`common.json` に追加する。`en`:

```json
{
  "date.unknown": "Unknown date",
  "date.justNow": "just now"
}
```

`ja`:

```json
{
  "date.unknown": "日付不明",
  "date.justNow": "たった今"
}
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
cd apps/electron && npx vitest run src/lib/__tests__/smartDate.test.ts
```

期待: 4 件 PASS。

- [ ] **Step 5: 全テストを確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck EXIT 0、既知の赤 3 件のみ。

英語の相対表記は 1 文字も変えていないため、`formatRelativeDate` を使う `SourceReader.tsx` / `rowMeta.ts` / `Actionables.tsx` 経由の `ByText()` アサートも無傷のはずである。**ここで英語側のテストが落ちたら、英語の分岐に誤って手を入れている。** `git diff` で `formatRelativeDate` の既存本体が変わっていないことを確認する。

- [ ] **Step 6: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/lib/smartDate.ts \
        apps/electron/src/lib/__tests__/smartDate.test.ts apps/electron/src/i18n
git commit -F - <<'EOF'
feat: 日付と時刻を UI 言語に合わせて整形する

ロケールは OS ではなく UI 言語から決める。設定で English を
選んだ利用者は、日本語 Windows でも英語の日付を期待するため。

相対表記は Intl.RelativeTimeFormat に置き換えた。手書きの
分岐を日本語向けに増やすより、言語ごとの規則を標準 API に
任せるほうが破綻しない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 13: カタログのキー一致を検証する構造テスト

日本語カタログを書き始める前に、欠落を機械的に検出できるようにする。

**Files:**
- Create: `apps/electron/src/i18n/__tests__/catalogue-parity.test.ts`

**Interfaces:**
- Consumes: Task 1 の `NAMESPACES`、Task 7〜12 で作った全カタログ
- Produces: なし（テストのみ）

- [ ] **Step 1: テストを書く**

`apps/electron/src/i18n/__tests__/catalogue-parity.test.ts`:

```ts
/**
 * Catalogue parity — the ja catalogues must carry exactly the keys the en ones
 * do. A missing key silently renders English (fallbackLng), which is easy to
 * ship without noticing; a surplus key is dead weight or a typo.
 *
 * This test is expected to FAIL for every namespace until Task 14 writes the
 * Japanese translations. That is the point: it is the checklist.
 */

import { describe, it, expect } from 'vitest'
import { NAMESPACES } from '../index'

import enCommon from '../locales/en/common.json'
import jaCommon from '../locales/ja/common.json'
import enLayout from '../locales/en/layout.json'
import jaLayout from '../locales/ja/layout.json'
import enLibrary from '../locales/en/library.json'
import jaLibrary from '../locales/ja/library.json'
import enDevice from '../locales/en/device.json'
import jaDevice from '../locales/ja/device.json'
import enSettings from '../locales/en/settings.json'
import jaSettings from '../locales/ja/settings.json'
import enToday from '../locales/en/today.json'
import jaToday from '../locales/ja/today.json'

const CATALOGUES: Record<string, { en: object; ja: object }> = {
  common: { en: enCommon, ja: jaCommon },
  layout: { en: enLayout, ja: jaLayout },
  library: { en: enLibrary, ja: jaLibrary },
  device: { en: enDevice, ja: jaDevice },
  settings: { en: enSettings, ja: jaSettings },
  today: { en: enToday, ja: jaToday }
}

/**
 * i18next appends a plural suffix to the key (`_one`, `_other`). English has two
 * forms, Japanese only `other`, so comparing raw keys would report a false
 * mismatch on every counted string. Compare the base keys instead.
 */
function baseKeys(catalogue: object): Set<string> {
  return new Set(Object.keys(catalogue).map((k) => k.replace(/_(one|other|zero|two|few|many)$/, '')))
}

describe('catalogue parity', () => {
  it('covers every declared namespace', () => {
    expect(Object.keys(CATALOGUES).sort()).toEqual([...NAMESPACES].sort())
  })

  for (const ns of Object.keys(CATALOGUES)) {
    it(`${ns}: ja has no missing keys`, () => {
      const en = baseKeys(CATALOGUES[ns].en)
      const ja = baseKeys(CATALOGUES[ns].ja)
      const missing = [...en].filter((k) => !ja.has(k)).sort()
      expect(missing).toEqual([])
    })

    it(`${ns}: ja has no surplus keys`, () => {
      const en = baseKeys(CATALOGUES[ns].en)
      const ja = baseKeys(CATALOGUES[ns].ja)
      const surplus = [...ja].filter((k) => !en.has(k)).sort()
      expect(surplus).toEqual([])
    })
  }

  it("every Japanese plural key uses only the 'other' form", () => {
    for (const ns of Object.keys(CATALOGUES)) {
      const badForms = Object.keys(CATALOGUES[ns].ja).filter((k) => /_(one|zero|two|few|many)$/.test(k))
      expect(badForms, `${ns} has non-'other' plural forms`).toEqual([])
    }
  })
})
```

- [ ] **Step 2: 失敗の内容を確認する**

```bash
cd apps/electron && npx vitest run src/i18n/__tests__/catalogue-parity.test.ts
```

期待: `ja has no missing keys` が全ネームスペースで FAIL し、**欠けているキーの一覧が出力される**。これが Task 14 の作業リストになる。出力を保存しておく:

```bash
cd apps/electron
npx vitest run src/i18n/__tests__/catalogue-parity.test.ts 2>&1 | tee ../../missing-keys.txt
```

- [ ] **Step 3: コミット**

このテストは意図的に赤いままコミットする。次のタスクで緑にする。

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/i18n/__tests__/catalogue-parity.test.ts
git commit -F - <<'EOF'
test: 英語と日本語のカタログのキー一致を検証する

日本語訳の欠落は fallbackLng によって静かに英語表示になるため、
目視では見つからない。このテストが翻訳作業のチェックリストになる。

複数形は基底キーで比較する。英語は _one/_other の2形、日本語は
_other のみなので、キーをそのまま比較すると全ての可算文字列が
不一致として報告されてしまう。

このコミットの時点では意図的に赤い。次のコミットで緑にする。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 14: 日本語カタログを書く

英語カタログが固まったので、翻訳に専念する。**コンポーネントには一切触らない。**

**Files:**
- Modify: `apps/electron/src/i18n/locales/ja/*.json`（6 ファイル）

**Interfaces:**
- Consumes: Task 13 の欠落キー一覧
- Produces: 完成した日本語カタログ

- [ ] **Step 1: ネームスペースごとに翻訳する**

`common` → `layout` → `settings` → `device` → `today` → `library` の順。各ファイルを書いたら、そのネームスペースだけ検証する:

```bash
cd apps/electron && npx vitest run src/i18n/__tests__/catalogue-parity.test.ts -t "layout"
```

翻訳の方針:

- **敬体（ます・です）を使う。** アプリの UI として自然であり、英語の中立的な語調に対応する
- **ボタンは体言止めか動詞の終止形。** 「保存」「キャンセル」「削除」。「保存します」とはしない
- **専門用語はカタカナか英語のまま。** `Transcription` → 「文字起こし」、`Gemini` / `Ollama` / `ASR` はそのまま
- **助数詞を正しく選ぶ。** 録音は「件」、ファイルは「個」ではなく「件」、人は「名」
- **`{{count}}` の位置に注意する。** 「{{count}}件の録音」であって「録音{{count}}件」ではない箇所と、その逆がある。原文の語順に引きずられない

例（`ja/layout.json`）:

```json
{
  "titleBar.minimize": "最小化",
  "titleBar.maximize": "最大化",
  "titleBar.close": "閉じる",
  "operations.title": "処理状況",
  "operations.empty": "実行中の処理はありません",
  "notifications.label": "通知",
  "activityLog.label": "操作ログ",
  "assistant.title": "アシスタント",
  "assistant.loading": "アシスタントを読み込んでいます...",
  "stats.recordings_other": "{{count}}件の録音"
}
```

- [ ] **Step 2: キー一致が緑になることを確認する**

```bash
cd apps/electron && npx vitest run src/i18n/__tests__/catalogue-parity.test.ts
```

期待: 全 PASS。

- [ ] **Step 3: 英語の回帰網が無傷であることを確認する**

```bash
cd apps/electron && npm run typecheck && npm run test:run
```

期待: typecheck EXIT 0、既知の赤 3 件のみ。**日本語カタログを足しただけなのでテストは英語で動いており、1 件も増減しないはずである。** 増減したら、誤って英語カタログかコンポーネントを触っている。

- [ ] **Step 4: コミット**

ネームスペースごとにコミットする。例:

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src/i18n/locales/ja/layout.json
git commit -F - <<'EOF'
feat: 共通レイアウトの日本語訳を追加する

敬体で統一し、ボタンは体言止めにした。助数詞は録音を「件」で数える。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 15: 実機確認とレイアウト調整

**Files:**
- Modify: 崩れが見つかった箇所のみ

**Interfaces:**
- Consumes: Task 1〜14 のすべて
- Produces: なし（確認と修正）

- [ ] **Step 1: 開発モードで起動する**

```bash
cd D:/ClaudeCode/HiDock-NEXT2/apps/electron
export ELECTRON_GET_USE_PROXY=true
export GLOBAL_AGENT_HTTPS_PROXY=http://127.0.0.1:3129
npm run dev
```

- [ ] **Step 2: 言語を切り替えて各画面を見る**

設定 → Appearance → 日本語 に切り替え、次を確認する:

- Library（一覧・リーダー・絞り込み・一括操作）
- Device
- Today
- Settings
- 共通レイアウト（タイトルバー・サイドバー・通知・操作ログ・処理状況）

見るべき点:

1. 英語のまま残っている箇所がないか（抽出漏れ）
2. ボタンやタブから文字がはみ出していないか（日本語は英語より幅を取ることが多い）
3. 1 行のはずが 2 行になって高さが崩れていないか
4. 日付が `2026年9月22日` 形式になっているか
5. 件数表示の助数詞が自然か

- [ ] **Step 3: 崩れた箇所だけ直す**

**事前に全部直そうとしない。** 実際に崩れた箇所のみ、`min-w-*` や `whitespace-nowrap`、`text-xs` への変更で対処する。

- [ ] **Step 4: 英語に戻して崩れていないことを確認する**

設定 → Appearance → English に戻し、Step 3 の修正が英語表示を壊していないことを確認する。

- [ ] **Step 5: インストーラをビルドして確認する**

```bash
cd D:/ClaudeCode/HiDock-NEXT2/apps/electron
export ELECTRON_GET_USE_PROXY=true
export GLOBAL_AGENT_HTTPS_PROXY=http://127.0.0.1:3129
npm run build:win
```

期待: EXIT 0、`dist/hidock-next-2.0.0-setup.exe` が生成される。

- [ ] **Step 6: コミット**

```bash
cd D:/ClaudeCode/HiDock-NEXT2
git add apps/electron/src
git commit -F - <<'EOF'
fix: 日本語表示で崩れたレイアウトを調整する

実機で確認して崩れた箇所のみ直した。英語表示に戻して
影響がないことも確認済み。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## 自己レビューの結果

計画を書き終えたあと、仕様書と突き合わせて確認した結果を記録する。

**仕様書の各節に対応するタスク:**

| 仕様書 | タスク |
| --- | --- |
| 3.1 依存関係 | Task 1 |
| 3.2 ディレクトリ構成 | Task 1（`common`）、Task 7〜11（各ネームスペース） |
| 3.3 キーの命名規則 | Global Constraints、Task 7 |
| 3.4 初期化の設定 | Task 1 |
| 3.5 複数形 | Task 7 Step 4、Task 10、Task 13 |
| 3.6 JSX の埋め込み | Task 7 Step 4 |
| 4.1 設定項目 | Task 3 |
| 4.2 OS ロケールの取得 | Task 2 |
| 4.3 テーマ機構の踏襲 | Task 2、Task 4、Task 5 |
| 4.4 設定画面 | Task 6 |
| 5 日付と時刻 | Task 12 |
| 6.1 抽出と翻訳の分離 | Task 7〜11（抽出）と Task 14（翻訳）を別タスクにした |
| 6.2 回帰判定の基準 | Global Constraints、各タスクの確認ステップ |
| 7 テスト | Task 2、Task 5、Task 13 |
| 8 ビルドと配布 | Global Constraints、Task 15 |
| 9 リスク | Global Constraints（9.4）、Task 15（9.2） |

**未着手として残る仕様書の範囲:** 2.2 節の Phase 2（Calendar / Chat / People / PersonDetail / Projects / Actionables / Explore / ContextGraph / MeetingDetail、main プロセスのネイティブメニュー）。これは仕様書が明示的に範囲外としているため、本計画にタスクを置かない。

**この計画で新たに判明し、仕様書に反映した事項:**

- 4.2 節 — `app:info` への `locale` 追加は成立しない（非同期 IPC がプリペイントに間に合わない）。`navigator.language` に変更した
- 4.4 節 — テーマセレクタは UI 上に存在しないため「隣に置く」ことはできない。`Appearance` カードを新設し、Assistant カードのセグメントボタンの形を踏襲する
- 7 節 — `useTheme` にフック単位のテストは無い。雛形になるのは `src/lib/__tests__/theme.test.ts`（フレームワーク非依存の中核のテスト）である
- 4.3 節 — ストアの追加先は `store/ui/useUIStore.ts`。`store/useUIStore.ts` は再エクスポートであり触らない

**自己レビューで見つけて計画を直した欠陥:**

- Task 12 — 当初は相対表記を丸ごと `Intl.RelativeTimeFormat` に置き換える内容だった。既存の英語は `5 min ago` / `3 h ago` / `2 wks ago` / `5 mo ago` / `2 yr ago` という独自の短縮形で、`Intl` の既定とほぼ全単位で食い違う。これでは日本語化とは無関係に英語の表示を変えてしまい、本設計の根幹（抽出は表示を変えない）に反する。英語の分岐を一切触らず、日本語のときだけ早期リターンで `Intl` に送る形に直した

**判断を人間に委ねる箇所（実装中に該当したら止めて確認する）:**

- Task 10 Step 3 — 複数形の統合で DOM のノード構造が変わる。完全一致のアサートは通るが、構造に依存したクエリが落ちる可能性がある
- Task 7〜11 全般 — 英語カタログへの抽出で `ByText()` が落ちた場合、原因が抽出ミス（カタログの文字列が元と違う）かノード構造の変化かを切り分ける。**テスト側を書き換えて通すことは、いずれの場合も禁止。** 前者はカタログを直し、後者は人間に確認する
